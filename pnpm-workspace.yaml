# Workspace layout mirroring the DSH monorepo shape the typert generator's
# analyzer expects (packages under packages/, tsconfig.host.json at the root).
packages:
  - packages/*

# Build scripts pnpm may run in this repository's installs.
allowBuilds:
  esbuild: true
